[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nana-risk"
version = "0.1.0"
description = "Repo risk analyzer core"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic<2",
    "typer",
    "rich",
]
